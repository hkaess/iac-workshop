# Loesung zu Uebung 2

Die Musterloesung zeigt drei zentrale Punkte:

- CPU und RAM werden ueber Variablen veraendert
- eine zweite Disk wird als eigener `disk`-Block angelegt
- Name, VM-ID und IP bleiben ueber `participant_id` und `participant_number` eindeutig

